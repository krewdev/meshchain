---
description: Show MeshChain testnet status and key addresses
---

Summarize MeshChain project status for the user:

1. Read `testnet/network.json` and `testnet/attestors.json` if present under the meshchain repo.
2. If `data/chain_state.json` exists, report height, supply, account count.
3. List: chain_id, Solana program id, site URL, how to run scanner and wallets.
4. Remind: TESTNET ONLY, tMESH has no value.

Keep the answer short with tables.
